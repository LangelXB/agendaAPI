components:
  schemas:
    User:
      {
        type: "object",
        properties:
          {
            idUser: { type: "integer", minimum: 1 },
            fullName: { type: "string" },
            phoneUser: { type: "string", example: "123456789" },
            emailUser: { type: "string", format: "email" },
          },
      }
